<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="primitive.css" />
    <title>URL Shortener</title>
  </head>
  <body>
    <div class="container">
      <h1 class="text-center">Cara Penggunaan</h1>
      <ol class="margin-bottom">
        <li>Masukkan link URL anda</li>
        <li>Click Shrink</li>
        <li>
          Hasil Shortened URL dapat dilihat / di-copy pada kolom Short URL
        </li>
        <li>
          Jumlah pengunjung yang meng-click URL anda dapat dilihat di kolom
          Clicks
        </li>
      </ol>
      <h1 class="text-center">URL Shrinker</h1>
      <form action="/shortUrls" method="post" class="split-form">
        <label for="fullUrl" class="screen-reader-text">URL</label>
        <input
          required
          placeholder="Enter URL"
          type="url"
          name="fullUrl"
          id="fullUrl"
        />
        <button type="submit">Shrink</button>
      </form>

      <table class="striped-table contain-table">
        <thead>
          <tr>
            <th>Full Url</th>
            <th>Short Url</th>
            <th>Clicks</th>
          </tr>
        </thead>
        <tbody>
          <% shortUrls.forEach(shortUrl => { %>
          <tr>
            <td>
              <a href="<%= shortUrl.full %> "> <%= shortUrl.full %> </a>
            </td>
            <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
            <td><a><%= shortUrl.clicks %> </a></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </body>
</html>
